naftiko: 1.0.0-alpha2
info:
  label: Azure Database for PostgreSQL REST API — Operations
  description: 'Azure Database for PostgreSQL REST API — Operations. 1 operations. Lead operation: Azure Database for PostgreSQL
    REST API List operations. Self-contained Naftiko capability covering one Microsoft Azure Postgresql business surface.'
  tags:
  - Microsoft Azure Postgresql
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_POSTGRESQL_API_KEY: MICROSOFT_AZURE_POSTGRESQL_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-postgresql-operations
    baseUri: https://management.azure.com
    description: Azure Database for PostgreSQL REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.DBforPostgreSQL-operations
      path: /providers/Microsoft.DBforPostgreSQL/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Database for PostgreSQL REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_POSTGRESQL_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-postgresql-operations-rest
    port: 8080
    description: REST adapter for Azure Database for PostgreSQL REST API — Operations. One Spectral-compliant resource per
      consumed operation, prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-dbforpostgresql/operations
      name: providers-microsoft-dbforpostgresql-operations
      description: REST surface for providers-Microsoft.DBforPostgreSQL-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Database for PostgreSQL REST API List operations
        call: microsoft-azure-postgresql-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-postgresql-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Database for PostgreSQL REST API — Operations. One tool per consumed operation, routed
      inline through this capability's consumes block.
    tools:
    - name: azure-database-postgresql-rest-api
      description: Azure Database for PostgreSQL REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-postgresql-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
